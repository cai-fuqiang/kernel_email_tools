---
globs: *
alwaysApply: true
---

# 内核邮件列表知识库 - JoyCode 完整开发规则

## 项目概览

| 属性 | 说明 |
|------|------|
| **项目名称** | kernel_email_tools |
| **类型** | 全栈 Web 应用（Python 后端 + React 前端）|
| **目标** | Linux 内核邮件列表知识库 + 芯片手册知识库，支持精确检索和 RAG 语义问答 |
| **数据源** | lore.kernel.org（git mirror）+ Intel SDM PDF |
| **架构** | 七层插件化（Collector → Parser → Chunker → Storage → Indexer → Retriever → QA）|
| **双引擎** | PostgreSQL GIN 全文索引 + pgvector 向量检索 |

---

## 技术栈

### 后端
- **语言**: Python 3.11+
- **框架**: FastAPI, Uvicorn
- **数据库**: PostgreSQL 16 + pgvector 扩展
- **ORM**: SQLAlchemy 2.0 async
- **驱动**: asyncpg
- **数据校验**: Pydantic v2
- **配置**: YAML (config/settings.yaml)
- **Embedding**: OpenAI text-embedding-3-small
- **LLM**: OpenAI GPT-4 / Anthropic Claude / 阿里千问 / MiniMax 海螺

### 前端
- **框架**: React 18 + TypeScript
- **构建**: Vite
- **样式**: Tailwind CSS 3
- **路由**: React Router v7

---

## 目录结构

```
kernel_email_tools/
├── config/
│   └── settings.yaml              # 唯一配置中心（所有连接串、API Key、开关）
├── src/                           # Python 后端
│   ├── collector/                 # 数据采集层
│   │   ├── base.py              # BaseCollector 抽象接口
│   │   └── git_collector.py     # GitCollector 实现
│   ├── parser/                    # 解析层
│   │   ├── base.py              # BaseParser + BaseManualParser 抽象接口
│   │   ├── email_parser.py       # RFC2822 邮件解析
│   │   ├── patch_extractor.py    # 补丁提取
│   │   ├── thread_builder.py     # 线程重建
│   │   ├── pdf_extractor.py      # PDF 解析
│   │   └── intel_sdm/            # Intel SDM 专用解析器
│   │       └── parser.py
│   ├── chunker/                   # 文档分片层
│   │   ├── base.py              # BaseChunker + BaseSectionChunker + BaseContentTypeChunker
│   │   ├── section_chunker.py    # 按章节分片（L1）
│   │   ├── content_type_chunker.py # 按内容类型分片（L2）
│   │   ├── instruction_chunker.py # 指令分片器
│   │   ├── table_chunker.py      # 表格分片器
│   │   ├── sliding_window.py     # 滑动窗口分片（L3）
│   │   └── pipeline.py           # 分片管道编排
│   ├── storage/                   # 存储层
│   │   ├── base.py              # BaseStorage + BaseEmailStorage
│   │   ├── models.py            # EmailORM + TagORM + Pydantic 模型
│   │   ├── postgres.py           # PostgresStorage（邮件存储）
│   │   ├── tag_store.py         # TagStore 标签管理
│   │   ├── translation_cache.py  # TranslationCache ORM + TranslationCacheStore
│   │   ├── document_models.py    # DocumentChunkModel ORM
│   │   └── document_store.py     # DocumentStorage（手册存储）
│   ├── indexer/                  # 索引层
│   │   ├── base.py              # BaseIndexer
│   │   ├── fulltext.py          # 全文索引构建
│   │   └── vector.py             # 向量索引构建
│   ├── retriever/                # 检索层
│   │   ├── base.py              # SearchQuery + SearchHit + BaseRetriever
│   │   ├── keyword.py            # KeywordRetriever (GIN 全文)
│   │   ├── semantic.py           # SemanticRetriever (向量)
│   │   ├── hybrid.py             # HybridRetriever (混合编排)
│   │   └── manual.py             # ManualRetriever (手册检索)
│   ├── qa/                       # 问答层
│   │   ├── base.py              # Answer + SourceReference + BaseQA
│   │   ├── rag_qa.py            # 邮件 RAG Pipeline
│   │   └── manual_qa.py          # 手册 RAG Pipeline
│   ├── translator/               # 翻译层（新增）
│   │   ├── base.py              # BaseTranslator
│   │   └── google_translator.py # Google 翻译实现
│   └── api/
│       └── server.py            # FastAPI 服务入口
├── scripts/                       # 运维脚本
│   ├── collect.py               # 数据采集入口
│   ├── index.py                 # 索引构建入口
│   ├── ingest_manual.py          # 手册导入入口
│   ├── migrate_add_message_id.py # DB 迁移：translation_cache 添加 message_id 列
│   └── serve.py                 # 服务启动入口
├── web/                          # React 前端
│   ├── src/
│   │   ├── pages/               # 页面组件
│   │   │   ├── SearchPage.tsx   # 邮件搜索页（含标签筛选）
│   │   │   ├── AskPage.tsx      # 邮件问答页（含标签筛选）
│   │   │   ├── TagsPage.tsx     # 标签管理页
│   │   │   ├── TranslationsPage.tsx # 翻译管理页（已缓存线程+标签+自动轮询）
│   │   │   ├── ManualSearchPage.tsx  # 手册搜索页
│   │   │   └── ManualAskPage.tsx     # 手册问答页
│   │   ├── components/          # 通用组件
│   │   │   ├── ThreadDrawer.tsx # 线程抽屉（含翻译、引用行渲染、PatchDiffBlock）
│   │   │   ├── TagFilter.tsx    # 标签筛选组件
│   │   │   ├── TagManager.tsx   # 标签管理组件
│   │   │   └── EmailTagEditor.tsx # 邮件标签编辑器
│   │   ├── layouts/            # 布局组件
│   │   │   └── MainLayout.tsx
│   │   └── api/                # API 客户端
│   │       ├── client.ts       # fetch 封装 + API 函数
│   │       └── types.ts        # TypeScript 类型定义
│   └── dist/                   # 构建产物 (FastAPI 挂载到 /app/)
├── tests/                        # pytest 测试
└── manuals/                      # 芯片手册 PDF 存放目录
```

---

## 数据模型

### EmailORM 核心字段

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | int | 主键，自增 |
| `message_id` | str | 唯一约束，邮件标识 |
| `subject` | str | 邮件主题 |
| `sender` | str | 发件人（格式: "Name <email>"）|
| `date` | datetime | 发送时间，带时区 |
| `in_reply_to` | str | 回复目标 Message-ID |
| `references` | list[str] | 线程引用链 |
| `body` | str | 邮件正文（清洗后）|
| `body_raw` | str | 原始正文 |
| `patch_content` | str | 提取的补丁内容 |
| `has_patch` | bool | 是否包含补丁 |
| `list_name` | str | 邮件列表名（如 linux-mm）|
| `thread_id` | str | 线程根 ID |
| `epoch` | int | 时间段编号 |
| `tags` | list[str] | 标签列表（最多 16 个）|
| `search_vector` | TSVECTOR | 全文搜索向量 |

### TagORM 核心字段（标签管理）

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | int | 主键，自增 |
| `name` | str | 标签名称（唯一）|
| `parent_id` | int | 父标签 ID（支持层级标签，NULL 表示顶级）|
| `color` | str | 标签颜色（十六进制，如 #6366f1）|
| `created_at` | datetime | 创建时间 |

### TranslationCache 核心字段（翻译缓存）

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | int | 主键，自增 |
| `source_hash` | str(64) | 原文 SHA256 哈希（唯一索引）|
| `source_text` | text | 原文内容 |
| `translated_text` | text | 翻译结果 |
| `source_lang` | str(10) | 源语言（默认 auto）|
| `target_lang` | str(10) | 目标语言（默认 zh-CN）|
| `message_id` | str(512) | 关联邮件 Message-ID（nullable, indexed）|
| `created_at` | datetime | 创建时间 |

- 通过 `message_id` 关联 `emails` 表，支持按 thread 聚合翻译统计和标签展示
- 翻译时自动写入 `message_id`；已有缓存命中时 backfill 补充关联

### DocumentChunkModel 核心字段（手册分片）

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | int | 主键 |
| `chunk_id` | str | 分片唯一 ID（如 intel_sdm:3.2.1:001）|
| `manual_type` | str | 手册类型（如 intel_sdm）|
| `manual_version` | str | 手册版本号 |
| `volume` | str | 卷标识 |
| `chapter` | str | 章标识 |
| `section` | str | 节编号 |
| `section_title` | str | 节标题 |
| `content_type` | str | 内容类型（text/table/instruction/register/pseudocode/figure）|
| `content` | str | 分片正文 |
| `context_prefix` | str | 层级路径前缀（用于检索增强）|
| `content_zh` | str | 中文翻译缓存 |
| `page_start` | int | 起始页码（0-based）|
| `page_end` | int | 结束页码（0-based）|
| `token_count` | int | token 数量 |
| `extra_data` | dict | 扩展元数据（JSON）|
| `search_vector` | TSVECTOR | 全文搜索向量 |
| `embedding` | list | 向量嵌入 |

### 索引策略
- **邮件表**: GIN(search_vector), GIN(tags), 复合(list_name, thread_id), 唯一(message_id)
- **标签表**: name 唯一约束, parent_id 索引
- **翻译缓存表**: 唯一(source_hash), 索引(message_id)
- **手册表**: GIN(search_vector), 复合(manual_type, content_type)

---

## 架构层次与调用关系

### 数据流向

```
┌─────────────────────────────────────────────────────────────────┐
│                        API Layer (FastAPI)                      │
│  /api/search  /api/ask  /api/thread  /api/manual/search  ...    │
└─────────────────────────┬───────────────────────────────────────┘
                          │
┌─────────────────────────▼───────────────────────────────────────┐
│                      QA Layer (RAG Pipeline)                    │
│  RagQA / ManualQA                                               │
│  Pipeline: 问题 → 检索 → 上下文构建 → LLM 生成                  │
└─────────────────────────┬───────────────────────────────────────┘
                          │
┌─────────────────────────▼───────────────────────────────────────┐
│                    Retriever Layer                              │
│  KeywordRetriever  SemanticRetriever  HybridRetriever           │
│  ManualRetriever                                                │
│  功能: 查询意图路由 + 多引擎结果融合                            │
└─────────────────────────┬───────────────────────────────────────┘
                          │
┌─────────────────────────▼───────────────────────────────────────┐
│                    Indexer Layer                                │
│  FulltextIndexer: GIN 全文索引维护                              │
│  VectorIndexer: pgvector 向量索引维护（Phase 2）                │
└─────────────────────────┬───────────────────────────────────────┘
                          │
┌─────────────────────────▼───────────────────────────────────────┐
│                    Storage Layer                                │
│  PostgresStorage (邮件) / DocumentStorage (手册)                │
│  TagStore / TranslationCache                                    │
└─────────────────────────┬───────────────────────────────────────┘
                          │
┌─────────────────────────▼───────────────────────────────────────┐
│  Chunker Layer          │          Parser Layer                 │
│  ChunkPipeline          │          EmailParser / IntelSDMParser │
│  (L1章节 → L2类型 → L3滑动窗口)    ThreadBuilder / PatchExtractor│
└─────────────────────────┬───────────────────────────────────────┘
                          │
┌─────────────────────────▼───────────────────────────────────────┐
│                    Collector Layer                              │
│  GitCollector（从 lore.kernel.org git mirror 采集）             │
│  支持多 epoch、本地 channel 模式                                │
└─────────────────────────────────────────────────────────────────┘
```

### 模块间依赖关系

```
┌─────────────────────────────────────────────────────────────────┐
│  api/server.py                                                  │
│  ├── qa/rag_qa.py ─────────→ retriever/hybrid.py               │
│  │                             ├── keyword.py                   │
│  │                             ├── semantic.py                  │
│  │                             └── manual.py ───→ storage/     │
│  ├── qa/manual_qa.py ───────→ retriever/manual.py              │
│  ├── translator/google_translator.py                            │
│  └── 全局组件初始化 (lifespan)                                   │
│      ├── PostgresStorage / DocumentStorage                     │
│      ├── TagStore / TranslationCache                            │
│      └── HybridRetriever / ManualRetriever                     │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│  scripts/ 入口脚本                                              │
│  ├── collect.py ─────→ collector/git_collector.py              │
│  │                      └── parser/email_parser.py             │
│  ├── index.py ───────→ collector → parser → storage → indexer  │
│  ├── ingest_manual.py → parser/intel_sdm/parser.py             │
│  │                      └── chunker/pipeline.py                 │
│  │                      └── storage/document_store.py          │
│  └── serve.py ───────→ api/server.py (uvicorn)                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## API 接口

### 核心端点

| 端点 | 方法 | 描述 |
|------|------|------|
| `/api/search` | GET | 邮件全文搜索（支持过滤、分页）|
| `/api/ask` | GET | RAG 问答（邮件上下文）|
| `/api/thread/{thread_id}` | GET | 获取完整邮件线程 |
| `/api/stats` | GET | 邮件数据库统计 |

### 标签管理 API

| 端点 | 方法 | 描述 |
|------|------|------|
| `/api/tags` | POST | 创建标签 |
| `/api/tags` | GET | 获取标签树（树形结构）|
| `/api/tags/stats` | GET | 获取标签统计 |
| `/api/tags/{tag_id}` | DELETE | 删除标签（级联删除子标签）|
| `/api/email/{message_id}/tags` | GET | 获取邮件标签 |
| `/api/email/{message_id}/tags` | POST | 添加标签（最多 16 个）|
| `/api/email/{message_id}/tags/{tag_name}` | DELETE | 删除邮件标签 |

### 翻译 API

| 端点 | 方法 | 描述 |
|------|------|------|
| `/api/translate` | POST | 单条翻译 |
| `/api/translate/batch` | POST | 批量翻译（最多 50 条，支持 message_id 关联）|
| `/api/translate/threads` | GET | 获取已翻译线程列表（含标签）|
| `/api/translate/health` | GET | 翻译服务健康检查 |
| `/api/translate/cache` | DELETE | 清除翻译缓存 |
| `/api/translate/manual` | PUT | 保存人工翻译 |

### 芯片手册 API

| 端点 | 方法 | 描述 |
|------|------|------|
| `/api/manual/search` | GET | 手册文档搜索 |
| `/api/manual/ask` | GET | 手册 RAG 问答 |
| `/api/manual/stats` | GET | 手册数据库统计 |

---

## 代码风格

### Python 规范

| 类型 | 规则 | 示例 |
|------|------|------|
| 文件 | snake_case | `git_collector.py` |
| 类 | PascalCase | `GitCollector`, `BaseRetriever` |
| 函数/方法 | snake_case | `build_thread_tree()` |
| 常量 | UPPER_SNAKE_CASE | `DEFAULT_EMBEDDING_MODEL` |
| 抽象基类 | Base 前缀 | `BaseCollector`, `BaseStorage` |

- 所有抽象基类使用 `abc.ABC` + `@abstractmethod`
- 类型注解必须完整，包括函数签名和返回值
- 使用 Pydantic `BaseModel` 做数据校验和序列化
- 异步优先：FastAPI 路由和数据库操作用 async/await
- 日志使用 Python `logging` 模块，格式: `%(asctime)s [%(levelname)s] %(name)s: %(message)s`
- 禁止裸 `print()`，开发和生产环境统一使用 logging

### TypeScript/React 规范

| 类型 | 规则 | 示例 |
|------|------|------|
| 变量/函数 | camelCase | `askQuestion`, `searchEmails` |
| 组件/类型 | PascalCase | `AskPage`, `AskResponse` |
| 文件 | camelCase / kebab-case | `client.ts`, `types.ts` |
| Props 接口 | PascalCase + Props 后缀 | `SearchPageProps` |
| 样式 | Tailwind 原子类 | `className="px-4 py-2"` |

- 组件默认导出 (`export default`)
- Props 定义接口
- 严格模式 (`strict: true`)，禁止 `any` 类型
- 前端 console 仅用于开发调试

---

## 架构原则

### 1. 插件化设计
每层通过抽象基类定义接口契约，实现类可替换：
- 新增数据源 → 实现 `BaseCollector`
- 新增存储后端 → 实现 `BaseStorage`
- 新增检索策略 → 实现 `BaseRetriever`
- 新增分片策略 → 实现 `BaseChunker`

### 2. 配置外置
所有配置集中在 `config/settings.yaml`：
- 数据库连接 (email, manual)
- API Key / Model 名称
- 功能开关 (vector.enabled, cache_enabled)
- 代码零硬编码

### 3. 双引擎检索
- **精确引擎**: PostgreSQL GIN 全文索引，关键词精确匹配 + 分页返回全量
- **语义引擎**: pgvector 向量相似度，top-K 召回（Phase 2 启用）
- **混合编排**: `HybridRetriever` 自动路由 + RRF 融合
  - 短查询（≤2词）→ 仅关键词检索
  - 问句（以问号结尾或含问句关键词）→ 关键词 + 语义检索

### 4. 分层分片策略（手册）
```
L1 章节分片 ──→ L2 内容类型分片 ──→ L3 滑动窗口分片
(BaseSectionChunker)  (BaseContentTypeChunker)  (滑动窗口)
```

### 5. 批量操作
- 写入数据库必须分批（500 条/批）
- 禁止逐条 for 循环 insert

### 6. 幂等性
- 采集和索引基于 `message_id` / `chunk_id` 去重
- 支持重复执行不产生重复数据

### 7. 异步优先
- FastAPI 路由使用 async/await
- 数据库操作用 SQLAlchemy async
- LLM 调用异步化

### 8. Session 管理（重要）
- **禁止长生命周期 session**：不得在应用启动时创建单一 AsyncSession 并全局复用
- **请求级 session**：存储类应接受 `session_factory`（`async_sessionmaker`），每次操作通过 `async with self.session_factory() as session:` 创建新 session
- **已踩坑案例**：`TranslationCacheStore` 曾持有固定 session，运行一段时间后 session 过期导致查询静默返回空
- **TagStore 待重构**：当前仍使用固定 session，后续需同步改为 session_factory 模式
- `PostgresStorage.session_factory` 类型为 `async_sessionmaker`，支持 `async with session_factory() as session:` 上下文管理

### 9. 标签系统
- 支持父子层级标签（树形结构）
- 单封邮件最多 16 个标签
- 标签名称全局唯一
- 删除父标签时级联删除所有子标签
- 标签过滤支持 `any`（任一匹配）和 `all`（全部匹配）两种模式

### 10. 邮件正文展示与翻译架构
- **数据源**: API `/api/thread/{id}` 返回 `body`（清洗后，无引用行）和 `body_raw`（原始正文，含引用行）
- **展示源选择**: 前端优先使用 `body_raw`，通过 `getDisplayBody()` 函数处理：
  1. `stripDiffAndSignature()` 从 body_raw 中剔除 diff/patch 块和签名（`-- ` 之后）
  2. 保留引用行（`>` 前缀），回退到 `body`（当 body_raw 为空时）
- **段落拆分**: `parseParagraphs()` 将正文拆分为 `ParagraphBlock`（`normal` | `quoted` 两种类型）
  - 纯引用段落 → `quoted` 类型
  - 混合段落按连续行类型自动拆分为多个子块
- **翻译策略**: `shouldTranslate()` 判断逻辑：
  - `quoted` 类型 → 不翻译，灰色斜体+左边框展示
  - 含中文 → 不翻译
  - 元数据行（Signed-off-by / Reviewed-by / Cc 等超过 80%）→ 不翻译
  - 其余 `normal` 类型 → 左右双栏对照翻译（原文 40% / 译文 60%）
- **diff 展示**: `PatchDiffBlock` 组件（独立于正文）
  - 默认折叠，标题栏显示文件数和增删行统计
  - 展开后按行着色：`+` 绿、`-` 红、`@@` 蓝、`diff --git` 黄、上下文灰
  - 带行号列
- **禁止重复**: body 中的 diff 内容已通过 `stripDiffAndSignature` 移除，仅在 PATCH 区域展示 `patch_content`

---

## 开发约束

1. **禁止跳过抽象层**: 新功能先定义/更新 `base.py` 接口，再写实现
2. **配置外置**: 禁止硬编码数据库连接、API Key、模型路径等
3. **错误处理**: 网络请求、数据库操作、LLM 调用必须有 try/except + 日志记录
4. **类型安全**: Python 全类型注解，TypeScript 严格模式
5. **文档字符串**: 公开类和方法必须有 Google 风格 docstring
6. **批量操作**: 写入数据库和向量索引时必须批量，禁止逐条
7. **标签上限**: 添加邮件标签时必须检查是否已达上限（16 个）
8. **构建产出**: 前端 `web/dist/` 由 FastAPI 挂载到 `/app/`

---

## 搜索过滤功能

### SearchQuery 参数

```python
@dataclass
class SearchQuery:
    text: str                      # 搜索关键词
    list_name: Optional[str]       # 邮件列表（channel）
    sender: Optional[str]          # 发件人模糊匹配
    date_from: Optional[datetime]  # 起始日期
    date_to: Optional[datetime]    # 结束日期
    has_patch: Optional[bool]      # 包含补丁
    tags: Optional[list[str]]      # 标签列表过滤
    tag_mode: str = "any"          # 标签匹配模式: "any" 或 "all"
    page: int = 1                  # 页码
    page_size: int = 50            # 每页数量
    top_k: int = 20                # 语义检索 top-K
```

### SearchHit 字段

```python
@dataclass
class SearchHit:
    message_id: str
    subject: str = ""
    sender: str = ""
    date: str = ""
    list_name: str = ""
    thread_id: str = ""
    has_patch: bool = False
    tags: list[str] = field(default_factory=list)  # 邮件标签列表
    score: float = 0.0
    snippet: str = ""           # 高亮匹配片段
    source: str = ""            # 来源：keyword / semantic / hybrid
```

---

## 常用命令

### 后端

```bash
# 安装依赖
pip install -e ".[dev,embedding]"

# 采集邮件数据（从 lore.kernel.org）
python scripts/collect.py --list linux-mm --epoch 0 --limit 100

# 采集邮件数据（本地多 channel 模式）
python scripts/collect.py --list linux-mm --local --epoch 0 --limit 100
python scripts/collect.py --all-channels --local

# 构建索引（入库 + 全文索引）
python scripts/index.py --list linux-mm --epoch 0
python scripts/index.py --list linux-mm --all-epochs

# 仅重建全文索引
python scripts/index.py --rebuild-fulltext

# 查看统计
python scripts/index.py --stats

# 导入芯片手册
python scripts/ingest_manual.py --pdf ./manuals/intel_sdm/sdm.pdf --store --drop-existing

# 启动 API 服务
python scripts/serve.py
# 或开发模式（热重载）
python scripts/serve.py --reload
# 或直接使用 uvicorn
uvicorn src.api.server:app --reload --port 8000

# 运行测试
pytest tests/ -v
```

### 前端

```bash
cd web && npm install

# 开发服务器（带 API 代理）
npm run dev

# 构建生产版本
npm run build

# 预览构建产物
npm run preview
```

---

## 开发工作流

1. **分析需求** → 创建/更新 `.joycode/plans/PLAN-xxx.md` 规约文档
2. **接口设计** → 更新对应模块的 `base.py` 抽象接口
3. **实现代码** → 实现具体功能，遵循架构原则
4. **端到端验证** → 用 scripts/ 入口脚本测试
5. **前端适配** → 更新 API client (`web/src/api/`) 和页面组件（如需要）
6. **提交代码** → git commit

---

## 扩展点（后续迭代）

- [ ] 向量检索启用（pgvector）- Phase 2
- [ ] LLM RAG 回答生成（需配置 API Key）
- [ ] 前端时间线/线程可视化
- [ ] 多数据源支持（NNTP/RSS/mbox 文件导入）
- [ ] 其他存储后端（Elasticsearch、Milvus）
- [ ] AI 标签建议（自动分析邮件内容推荐标签）
- [x] 邮件翻译（Google Translate + 缓存 + 人工校正 + 双语对照）
- [x] 翻译缓存关联邮件 message_id（支持按线程聚合）
- [x] 翻译管理页面（TranslationsPage：已缓存线程列表 + 标签展示）
- [x] TranslationCacheStore session_factory 重构（请求级 session，避免过期）
- [x] 翻译展示优化（引用行展示不翻译 + diff 去重 + 去除 EN/中文标签）
- [x] PATCH diff 折叠展示（默认折叠 + diff 着色 + 增删行统计）
- [ ] TagStore session_factory 重构（当前仍使用固定 session）